# SQL

## Difference between UNIQUE and Primary key

In primary key null is not allowed, however its allowed in unique key.

## Table deletion

In table deletion for foreign keys, you can't delete root if orphaned table will be there.